% this is the main function
function [s] = optimize(modelName, mue, lambda, maxTime)
 % pseudo-code
 % for i = 1 : µ
    % s = init(...);
    % s = evaluate(s);
    % pop{i} = s;
    % ...
  %end

  %while interminated (no progress anymore)
    % create lambda offspring
    % choose the µ best of the offspring (and parents)
end